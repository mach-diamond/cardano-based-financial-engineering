# Database Configuration (uses machdiamond_postgres container)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=fintech-test-suite
DB_USER=machdiamond
DB_PASSWORD=machdiamond_dev

# Server Configuration
PORT=3005

# ============================================================================
# Testnet Provider Configuration (for Preview and Preprod testnet operations)
# Provider priority: Kupo > Blockfrost > Maestro > Koios (fallback)
# ============================================================================

# Kupo Endpoints (self-hosted chain indexer - fastest if you have local access)
# These are full URLs to your Kupo instance
KUPO_ENDPOINT_PREVIEW=
KUPO_ENDPOINT_PREPROD=

# Ogmios Endpoints (WebSocket interface to cardano-node)
# Used for transaction submission and chain sync
OGMIOS_ENDPOINT_PREVIEW=
OGMIOS_ENDPOINT_PREPROD=

# Blockfrost API Keys (https://blockfrost.io/)
BLOCKFROST_PREVIEW=
BLOCKFROST_PREPROD=

# Maestro API Keys (https://www.gomaestro.org/)
MAESTRO_PREVIEW=
MAESTRO_PREPROD=

# Koios (https://koios.rest/) - No API key required for basic queries
